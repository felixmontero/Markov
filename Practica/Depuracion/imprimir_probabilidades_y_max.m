function imprimir_probabilidades_y_max(prob_vector, max_value, max_index)
    disp('Probabilidades por posición:');
    disp(prob_vector);
    disp(['La probabilidad más alta es ' num2str(max_value) ' en la zona ' num2str(max_index)]);
end
